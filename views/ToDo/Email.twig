{# views/emails/Email.twig #}
<!doctype html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Ваша заметка</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Arial, sans-serif;
            background-color: #f2f2f2;
        }

        .email-container {
            max-width: 600px;
            margin: 30px auto;
            background-color: #ffffff;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
        }

        .email-header {
            background: linear-gradient(135deg, #c31432 0%, #240b36 100%);
            color: #ffffff;
            text-align: center;
            padding: 24px 16px;
            font-size: 24px;
            font-weight: bold;
        }

        .email-body {
            padding: 24px 20px;
        }

        .note-content {
            font-size: 16px;
            color: #333333;
            line-height: 1.6;
            background: #f9f9f9;
            padding: 16px;
            border-radius: 12px;
            white-space: pre-wrap;
            margin-bottom: 20px;
        }

        .email-footer {
            font-size: 14px;
            color: #888888;
            text-align: center;
            border-top: 1px solid #dddddd;
            padding: 16px 20px;
        }

        @media (max-width: 620px) {
            .email-body {
                padding: 16px;
            }

            .note-content {
                font-size: 15px;
            }
        }
    </style>
</head>
<body>
<div class="email-container">
    <div class="email-header">У вас новая заметка</div>
    <div class="email-body">
        <div class="note-content">"{{ note_text }}"</div>
    </div>
    <div class="email-footer">
        Это автоматическое письмо от вашей системы заметок.
    </div>
</div>
</body>
</html>